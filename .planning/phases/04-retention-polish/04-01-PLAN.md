---
phase: 04-retention-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/patient/journal/trends/route.ts
  - src/components/profile/SymptomTrendChart.tsx
  - src/app/patient/profile/page.tsx
  - package.json
autonomous: true
requirements: [PROF-03]

must_haves:
  truths:
    - "Patient can view a line chart of symptom severity over time on the profile page"
    - "Chart renders data from existing SymptomJournal entries with dates on X-axis and severity 1-5 on Y-axis"
    - "Chart displays a health information disclaimer per AHPRA compliance"
  artifacts:
    - path: "src/app/api/patient/journal/trends/route.ts"
      provides: "GET endpoint returning symptom journal entries formatted for time-series chart"
      exports: ["GET"]
    - path: "src/components/profile/SymptomTrendChart.tsx"
      provides: "Recharts LineChart rendering severity over time"
      min_lines: 40
    - path: "src/app/patient/profile/page.tsx"
      provides: "Profile page with SymptomTrendChart integrated below journal entry form"
  key_links:
    - from: "src/components/profile/SymptomTrendChart.tsx"
      to: "/api/patient/journal/trends"
      via: "fetch in useEffect"
      pattern: "fetch.*api/patient/journal/trends"
    - from: "src/app/patient/profile/page.tsx"
      to: "src/components/profile/SymptomTrendChart.tsx"
      via: "import and render"
      pattern: "SymptomTrendChart"
---

<objective>
Add a symptom severity trend chart to the patient profile page, showing severity values over time as a line chart from existing SymptomJournal data.

Purpose: Patients gain visual evidence of their health trajectory over time, deepening the "continuously monitored" experience (PROF-03 enhancement from Phase 2 basic journal).
Output: Recharts LineChart component on profile page, backed by a trends API endpoint.
</objective>

<execution_context>
@/Users/tasmanstar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tasmanstar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-patient-experience/02-04-SUMMARY.md

<interfaces>
<!-- Existing SymptomJournal model from prisma/schema.prisma -->
model SymptomJournal {
  id        String   @id @default(cuid())
  patientId String
  patient   Patient  @relation(fields: [patientId], references: [id], onDelete: Cascade)
  severity  Int      // 1-5
  notes     String?
  createdAt DateTime @default(now())
}

<!-- Existing journal API from src/app/api/patient/journal/route.ts -->

// GET returns last 30 entries ordered desc
// POST creates entry with severity 1-5 + optional notes
// Both use x-patient-id header for auth

<!-- Existing profile page pattern from src/app/patient/profile/page.tsx -->

// Two-column layout: HealthProfileForm + SymptomJournalEntry
// Fetches from /api/patient/profile and /api/patient/journal on mount
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trends API endpoint and recharts install</name>
  <files>src/app/api/patient/journal/trends/route.ts, package.json</files>
  <read_first>
    - src/app/api/patient/journal/route.ts (existing journal API pattern)
    - prisma/schema.prisma (SymptomJournal model)
    - package.json (current deps)
  </read_first>
  <action>
1. Install recharts: `bun add recharts`

2. Create GET /api/patient/journal/trends endpoint:
   - Auth gate: x-patient-id header, 401 if missing
   - Query: prisma.symptomJournal.findMany where patientId, orderBy createdAt asc, take 90 (last 90 entries for ~3 months of daily logging)
   - Transform each entry to: { date: entry.createdAt (ISO string), severity: entry.severity, notes: entry.notes }
   - Return JSON array

This endpoint returns chronological data (asc) for chart rendering, unlike the existing journal GET which returns desc for the entry list.
</action>
<verify>
<automated>grep -n "x-patient-id" src/app/api/patient/journal/trends/route.ts && grep -n "orderBy.*asc" src/app/api/patient/journal/trends/route.ts && grep "recharts" package.json</automated>
</verify>
<acceptance_criteria> - grep "x-patient-id" src/app/api/patient/journal/trends/route.ts returns match (auth gate present) - grep "orderBy" src/app/api/patient/journal/trends/route.ts shows "asc" ordering - grep "recharts" package.json returns match (dependency installed) - grep "take.*90" src/app/api/patient/journal/trends/route.ts returns match (90 entry limit)
</acceptance_criteria>
<done>GET /api/patient/journal/trends returns chronologically ordered symptom journal entries for chart rendering, recharts installed</done>
</task>

<task type="auto">
  <name>Task 2: SymptomTrendChart component and profile page integration</name>
  <files>src/components/profile/SymptomTrendChart.tsx, src/app/patient/profile/page.tsx</files>
  <read_first>
    - src/app/patient/profile/page.tsx (current profile page layout)
    - src/components/profile/SymptomJournalEntry.tsx (existing journal component for style reference)
    - src/components/ui/card.tsx (shadcn Card for wrapper)
  </read_first>
  <action>
1. Create SymptomTrendChart.tsx ("use client"):
   - Props: { patientId: string }
   - State: trendData array, loading boolean
   - useEffect: fetch /api/patient/journal/trends with x-patient-id header, set trendData
   - Render: shadcn Card wrapping a Recharts ResponsiveContainer (height 300) containing:
     - LineChart with data={trendData}
     - XAxis dataKey="date" — format dates with toLocaleDateString("en-AU", { day: "numeric", month: "short" })
     - YAxis domain={[1, 5]} ticks={[1,2,3,4,5]} — label each tick: 1=Minimal, 2=Mild, 3=Moderate, 4=Severe, 5=Very Severe
     - Tooltip showing date, severity label, and notes (if present)
     - Line type="monotone" dataKey="severity" stroke="#3b82f6" (blue-500) strokeWidth={2} dot={{ r: 3 }}
   - Empty state: if trendData.length === 0, show "Log symptoms in your journal to see trends over time"
   - Loading state: skeleton placeholder
   - AHPRA disclaimer below chart: "This chart shows self-reported symptom data for informational purposes only. It is not a medical diagnosis."

2. Integrate into profile page:
   - Import SymptomTrendChart
   - Add below the existing SymptomJournalEntry section (after the journal entries column)
   - Pass patientId from the fetched profile data
   - Wrap in a section with heading "Symptom Trends"
     </action>
     <verify>
     <automated>grep -n "SymptomTrendChart" src/app/patient/profile/page.tsx && grep -n "LineChart" src/components/profile/SymptomTrendChart.tsx && grep -n "AHPRA\|informational purposes\|not a medical diagnosis" src/components/profile/SymptomTrendChart.tsx && bun run build 2>&1 | tail -5</automated>
     </verify>
     <acceptance_criteria>
   - grep "SymptomTrendChart" src/app/patient/profile/page.tsx returns match (component integrated)
   - grep "LineChart" src/components/profile/SymptomTrendChart.tsx returns match (recharts LineChart used)
   - grep "ResponsiveContainer" src/components/profile/SymptomTrendChart.tsx returns match (responsive wrapper)
   - grep -i "not a medical diagnosis\|informational purposes" src/components/profile/SymptomTrendChart.tsx returns match (AHPRA disclaimer)
   - grep "domain.*1.*5" src/components/profile/SymptomTrendChart.tsx returns match (Y-axis bounded 1-5)
   - bun run build exits 0
     </acceptance_criteria>
     <done>SymptomTrendChart renders a recharts line chart of severity over time on the profile page, with AHPRA disclaimer, and build passes</done>
     </task>

</tasks>

<verification>
- bun run build passes with no errors
- SymptomTrendChart component exists and uses recharts LineChart
- Trends API endpoint returns chronological data with auth gate
- Profile page imports and renders SymptomTrendChart
- AHPRA disclaimer present on chart component
</verification>

<success_criteria>
A patient viewing their profile page sees a line chart showing severity (1-5) over time from their logged journal entries, with an AHPRA-compliant health information disclaimer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retention-polish/04-01-SUMMARY.md`
</output>
