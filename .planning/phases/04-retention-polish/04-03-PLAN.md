---
phase: 04-retention-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/doctor/monitoring/route.ts
  - src/components/doctor/MonitoringQueue.tsx
autonomous: true
requirements: [DASH-02, PROF-03]

must_haves:
  truths:
    - "Doctor monitoring queue shows urgency trend direction (improving, stable, worsening) per patient based on recent check-in history"
    - "Trend indicator is visual (arrow icon + label) not just text"
    - "Trend is computed from last 3-5 check-in responses, not just the latest one"
  artifacts:
    - path: "src/app/api/doctor/monitoring/route.ts"
      provides: "Extended monitoring API with urgencyTrend field per patient"
    - path: "src/components/doctor/MonitoringQueue.tsx"
      provides: "MonitoringQueue with trend indicator arrows in each patient card"
  key_links:
    - from: "src/components/doctor/MonitoringQueue.tsx"
      to: "/api/doctor/monitoring"
      via: "fetch on mount"
      pattern: "fetch.*api/doctor/monitoring"
    - from: "src/app/api/doctor/monitoring/route.ts"
      to: "prisma.checkIn"
      via: "expanded check-in query for trend computation"
      pattern: "checkIns.*take.*[3-5]"
---

<objective>
Add urgency trend indicators to the doctor monitoring queue so doctors see whether each patient is improving, stable, or worsening over their recent check-ins — not just a point-in-time urgency snapshot.

Purpose: Doctors can prioritize patients who are trending worse even if their current urgency is routine (success criteria #3 for Phase 4).
Output: Trend arrows and labels on each MonitoringQueue patient card, backed by extended monitoring API data.
</objective>

<execution_context>
@/Users/tasmanstar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tasmanstar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-proactive-care-loop/03-04-SUMMARY.md
@.planning/phases/03-proactive-care-loop/03-02-SUMMARY.md

<interfaces>
<!-- Existing MonitoringPatient interface from src/components/doctor/MonitoringQueue.tsx -->
interface MonitoringPatient {
  id: string;
  name: string;
  email: string;
  knownConditions: string | null;
  lastCheckIn: { status: string; response: string | null; respondedAt: string | null; createdAt: string; } | null;
  lastConsultation: { urgencyLevel: string | null; symptoms: string | null; createdAt: string; } | null;
  effectiveUrgency: string;
  lastAgentActivity: { agentName: string; message: string; updatedAt: string; } | null;
}

<!-- Existing monitoring API pattern from src/app/api/doctor/monitoring/route.ts -->

// Fetches all active patients with nested checkIns (take: 1), consultations (take: 1), careTeamStatus
// Computes effectiveUrgency: "worse" response promotes to "urgent"
// Sorts by urgency order: emergency(0) > urgent(1) > routine(2) > self_care(3)

<!-- CheckIn.response values: "better" | "same" | "worse" | null -->

<!-- Existing badge variants from MonitoringQueue.tsx -->

// URGENCY_BADGE: emergency=destructive, urgent=default, routine=secondary, self_care=outline
// RESPONSE_COLOR: better=green-600, same=yellow-600, worse=red-600
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend monitoring API with urgency trend computation</name>
  <files>src/app/api/doctor/monitoring/route.ts</files>
  <read_first>
    - src/app/api/doctor/monitoring/route.ts (current full implementation)
    - prisma/schema.prisma (CheckIn model — response field)
  </read_first>
  <action>
Extend the existing GET /api/doctor/monitoring endpoint:

1. Change checkIns query from `take: 1` to `take: 5` to get the last 5 check-ins (still orderBy createdAt desc).

2. Add urgencyTrend computation after effectiveUrgency:
   - Extract responded check-ins only: filter where response is not null (skip pending/expired without response)
   - If fewer than 2 responded check-ins: urgencyTrend = "insufficient_data"
   - Otherwise, score each response: better = -1, same = 0, worse = +1
   - Compute average score across responded check-ins
   - Map average to trend:
     - average <= -0.3: "improving"
     - average >= 0.3: "worsening"
     - otherwise: "stable"

3. Add urgencyTrend to the returned patient object:

   ```
   urgencyTrend: "improving" | "stable" | "worsening" | "insufficient_data"
   ```

4. Keep lastCheckIn as the first element (most recent) — existing behavior preserved.

5. Do NOT change the sort order — urgency sort remains the same.
   </action>
   <verify>
   <automated>grep -n "urgencyTrend" src/app/api/doctor/monitoring/route.ts && grep -n "take.*5" src/app/api/doctor/monitoring/route.ts && grep -n "improving\|worsening\|stable\|insufficient_data" src/app/api/doctor/monitoring/route.ts</automated>
   </verify>
   <acceptance_criteria> - grep "urgencyTrend" src/app/api/doctor/monitoring/route.ts returns match (field added to response) - grep "take.*5" src/app/api/doctor/monitoring/route.ts returns match (expanded check-in query) - grep "improving" src/app/api/doctor/monitoring/route.ts returns match (trend category) - grep "worsening" src/app/api/doctor/monitoring/route.ts returns match (trend category) - grep "stable" src/app/api/doctor/monitoring/route.ts returns match (trend category) - grep "insufficient_data" src/app/api/doctor/monitoring/route.ts returns match (fallback) - The existing effectiveUrgency computation and sort order remain unchanged
   </acceptance_criteria>
   <done>Monitoring API returns urgencyTrend per patient based on last 5 check-in responses</done>
   </task>

<task type="auto">
  <name>Task 2: Add trend indicators to MonitoringQueue cards</name>
  <files>src/components/doctor/MonitoringQueue.tsx</files>
  <read_first>
    - src/components/doctor/MonitoringQueue.tsx (full current implementation)
  </read_first>
  <action>
Extend MonitoringQueue component:

1. Add urgencyTrend to MonitoringPatient interface:

   ```
   urgencyTrend: "improving" | "stable" | "worsening" | "insufficient_data";
   ```

2. Import TrendingUp, TrendingDown, Minus from lucide-react (alongside existing imports).

3. Create TREND_INDICATOR map:

   ```
   const TREND_INDICATOR: Record<string, { icon: LucideIcon; label: string; className: string }> = {
     improving: { icon: TrendingUp, label: "Improving", className: "text-green-600" },
     stable: { icon: Minus, label: "Stable", className: "text-yellow-600" },
     worsening: { icon: TrendingDown, label: "Worsening", className: "text-red-600" },
     insufficient_data: { icon: Minus, label: "Not enough data", className: "text-muted-foreground" },
   };
   ```

4. Add trend indicator to each patient card — position it next to the urgency Badge in the Patient Info column (first column of the 4-column grid):
   - After the existing Badge, add a div with:
     - The trend icon (w-4 h-4) and label text (text-xs)
     - Color from TREND_INDICATOR className
   - Example layout:
     ```
     <Badge>Urgent</Badge>
     <div className="flex items-center gap-1 mt-1">
       <TrendIcon className={`w-4 h-4 ${trendInfo.className}`} />
       <span className={`text-xs ${trendInfo.className}`}>{trendInfo.label}</span>
     </div>
     ```

5. Do NOT change loading, empty state, or card grid layout — only add the trend indicator.
   </action>
   <verify>
   <automated>grep -n "urgencyTrend" src/components/doctor/MonitoringQueue.tsx && grep -n "TrendingUp\|TrendingDown" src/components/doctor/MonitoringQueue.tsx && grep -n "TREND_INDICATOR\|improving\|worsening" src/components/doctor/MonitoringQueue.tsx && bun run build 2>&1 | tail -5</automated>
   </verify>
   <acceptance_criteria> - grep "urgencyTrend" src/components/doctor/MonitoringQueue.tsx returns match (interface extended) - grep "TrendingUp" src/components/doctor/MonitoringQueue.tsx returns match (trend icon imported) - grep "TrendingDown" src/components/doctor/MonitoringQueue.tsx returns match (trend icon imported) - grep "TREND_INDICATOR" src/components/doctor/MonitoringQueue.tsx returns match (trend config map) - grep "Improving" src/components/doctor/MonitoringQueue.tsx returns match (trend label) - grep "Worsening" src/components/doctor/MonitoringQueue.tsx returns match (trend label) - Existing URGENCY_BADGE and RESPONSE_COLOR maps remain unchanged - bun run build exits 0
   </acceptance_criteria>
   <done>MonitoringQueue cards display trend arrows (improving/stable/worsening) next to urgency badges. Build passes.</done>
   </task>

</tasks>

<verification>
- bun run build passes with no errors
- Monitoring API returns urgencyTrend field for each patient
- MonitoringQueue renders trend direction icons with color-coded labels
- Trend is based on multiple check-in responses, not just the latest
</verification>

<success_criteria>
The doctor portal monitoring queue displays urgency trend indicators (improving, stable, worsening) per patient, computed from recent check-in response history, giving doctors pattern visibility beyond point-in-time status.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retention-polish/04-03-SUMMARY.md`
</output>
