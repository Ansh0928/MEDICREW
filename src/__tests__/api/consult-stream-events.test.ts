import { describe, it } from 'vitest';
// import { POST } from '@/app/api/consult/route';

describe('CONS-02: Consultation stream events', () => {
  it.todo('POST /api/consult streams agent events in SSE format');
  it.todo('POST /api/consult emits agent_message events with role and content');
  it.todo('POST /api/consult emits final recommendation event');
  it.todo('POST /api/consult emits done event on completion');
});
