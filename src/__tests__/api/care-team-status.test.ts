import { describe, it } from 'vitest';
// import { GET } from '@/app/api/patient/care-team-status/route';

describe('DASH-01: Care team status API', () => {
  it.todo('GET /api/patient/care-team-status returns per-agent status messages');
  it.todo('GET /api/patient/care-team-status returns 401 without authentication');
  it.todo('GET /api/patient/care-team-status returns empty statuses for new patient');
});
