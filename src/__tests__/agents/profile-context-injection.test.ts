import { describe, it } from 'vitest';
// import { buildPatientContext } from '@/lib/patient-context';

describe('PROF-02: Profile context injection', () => {
  it.todo('Patient medications are included in agent system prompt context');
  it.todo('Patient allergies are included in agent system prompt context');
  it.todo('Patient known conditions are included in agent system prompt context');
  it.todo('Empty profile fields are omitted from context string');
});
